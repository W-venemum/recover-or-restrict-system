{
  "id": "FEAT-004",
  "type": "test",
  "description": "Comprehensive automated tests (build-priority item 11). Unit tests for classifier, risk engine, behavioural patterns, decision engine, access-state transitions, and recovery-action selection; adapter tests for Razorpay HMAC signature verification and OpenRouter deterministic fallback; and integration/e2e tests (supertest) for the major API flows including the 7 seeded scenarios and the webhook path. Tests must be meaningful (fail if the covered logic is reverted). Depends on FEAT-001/002/003.",
  "status": "completed",
  "steps": [
    "server/test/classifier.test.ts: assert each realistic failure reason maps to the correct FailureClass and that a single transient failure is recoverable (does not by itself produce high risk / abuse).",
    "server/test/risk.test.ts: assert weighted+decayed scoring - e.g. many OLD failures score lower than the same number of RECENT ones (recency decay), confidence rises with more evidence, and each active signal yields an evidence entry. Assert bands.",
    "server/test/patterns.test.ts: feed event sequences that exhibit cancellation cycling, payment-failure cycling, renewal avoidance, grace-period extraction, and cancel->use->resubscribe; assert each detector fires with evidence and does NOT fire on benign sequences.",
    "server/test/decision.test.ts: table-driven - genuine transient+low risk -> RECOVER; concerning -> INTERVENE; repeated avoidance -> RESTRICT; high-confidence abuse -> SUSPEND + blacklist recommendation flag set; verify next-best action escalates based on prior failed attempts.",
    "server/test/accessState.test.ts: assert full transition table incl. successful payment restoring access, and that BLACKLIST_RECOMMENDED is only ever a recommendation (never auto-applied without merchant review).",
    "server/test/razorpay.test.ts: compute a real HMAC-SHA256 signature over a raw body with a secret and assert verifyWebhookSignature returns true; tamper body/signature -> false. Assert only real event names are handled.",
    "server/test/openrouter.test.ts: with no OPENROUTER_API_KEY, assert explainDecision + draftRecoveryMessage return deterministic non-empty text derived from evidence and never throw.",
    "server/test/revenue.test.ts: assert aggregates (revenue at risk, recovered, recovery rate, risk distribution, priority ordering) computed correctly from a fixture set.",
    "server/test/api.e2e.test.ts (supertest): seed an in-memory/temp DB, then GET /api/dashboard, GET /api/customers, GET /api/customers/:id assert the 7 scenarios resolve to intended outcomes (esp. scenario 7 RECOVER-not-blacklist vs scenario 6 SUSPEND+blacklist-recommended). POST webhook with invalid signature -> 401, valid -> 200, replay same event id -> idempotent. POST /review approve/reinstate updates access + writes audit_log.",
    "Ensure tests use a temporary/isolated DATABASE_PATH so they don't clobber the demo DB."
  ],
  "acceptance_criteria": [
    "`npm test` runs vitest and ALL tests pass",
    "Coverage spans classifier, risk, patterns, decision, access state, recovery selection, both adapters, revenue, and API e2e incl. webhook signature + idempotency",
    "Tests are behavioural: reverting a rule in the engine causes a test to fail (no vacuous/static-only assertions)"
  ],
  "verification": [
    "Run `npm test` and paste the full passing output"
  ],
  "blocked_reason": null,
  "findings": "Added 9 vitest suites under server/test/ (116 behavioural tests, all passing). Coverage: classifier.test.ts (20) — realistic reason->FailureClass mapping + single-transient-stays-recoverable + repetition escalation; risk.test.ts (8) — recency decay (recent > same count old; ~half at one 30d half-life), confidence grows with evidence, one evidence entry per active signal kind, bands, success/tenure credit lower score; patterns.test.ts (11) — all 5 detectors fire-with-evidence on positive sequences and stay silent on benign ones; decision.test.ts (15) — table-driven RECOVER/INTERVENE/RESTRICT/SUSPEND + blacklistRecommended flag + next-best-action escalation (delayed_retry->upi_payment_link, retry->alternate_payment_method); accessState.test.ts (16) — full transition table incl. successful-payment-restores-ACTIVE, and invariant that BLACKLIST_RECOMMENDED is never entered automatically for any non-SUSPEND outcome/state; razorpay.test.ts (12) — real HMAC-SHA256 verify true, tampered body/sig/secret false, fail-closed on missing sig/secret, Buffer==string, only the 9 real event names accepted (unknown names throw); openrouter.test.ts (8) — no-key factory returns DeterministicExplainer, explainDecision/draftRecoveryMessage non-empty+deterministic+derived-from-evidence and never throw; revenue.test.ts (12) — bucketing (recovered/atRisk/pending/lost), recovery rate, risk distribution, priority ordering, predicted failures; api.e2e.test.ts (14) — in-memory (':memory:') DB seeded via real engine, all 7 scenarios resolve to intended outcomes (scenario 6 SUSPEND+blacklist vs scenario 7 RECOVER-not-blacklist), dashboard/customers/detail reads, webhook invalid-sig->401 / valid->200 / replay->duplicate, and /review approve_blacklist+restore_access+reinstate update access state and write audit_log rows.\n\nTests are isolated: e2e uses openDatabase(':memory:') and never touches server/data. No app code changed (all engine behaviour was already correct); the only non-test change was fixing .gitignore to also exclude SQLite WAL/SHM sidecar files (*.db-shm, *.db-wal, and data/) so the generated demo DB dir is never committed.\n\nGotcha for future features: when testing the raw-body webhook route with supertest, send the raw JSON STRING (not a Buffer) so superagent transmits the exact bytes the HMAC was computed over; sending a Buffer with a JSON content-type re-serialises and breaks signature verification.\n\nVerification — `npm test` from repo root:\n RUN  v2.1.9 server\n ✓ test/decision.test.ts (15 tests)\n ✓ test/razorpay.test.ts (12 tests)\n ✓ test/classifier.test.ts (20 tests)\n ✓ test/risk.test.ts (8 tests)\n ✓ test/patterns.test.ts (11 tests)\n ✓ test/revenue.test.ts (12 tests)\n ✓ test/accessState.test.ts (16 tests)\n ✓ test/openrouter.test.ts (8 tests)\n ✓ test/api.e2e.test.ts (14 tests)\n Test Files  9 passed (9)\n      Tests  116 passed (116)\n`npm run build` also passes (tsc), and the test files typecheck cleanly under strict mode."
}
